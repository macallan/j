{
  "name": "journal",
  "version": "1.0.0",
  "description": "my journal",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "FIRESTORE_EMULATOR_HOST=localhost:8080 node app.js",
    "prod-start": "npm install && PORT=8080 node app.js"
  },
  "author": "macallan",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "firebase-admin": "^9.6.0"
  }
}
